let initialState = {
    friendsList: [
        {id: 1, name: 'Aleksander'},
        {id: 2, name: 'Ta'},
        {id: 3, name: 'Veron'}
    ]
}

const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;